module temp

def outputBusinessCard(record)
	div(bgcolor="gray"){
		div record.lastName + " " + record.firstName;
		div record.street + " " + record.streetNumber;
		div record.postal + " " + record.city;
		div record.country;
	}
end