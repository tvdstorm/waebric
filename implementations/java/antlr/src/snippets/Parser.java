
public class Parser {
	
 public void moduleId() {
	 String path = "";
	 path += ".wae";
	 path += e.getText();
	 path += e.getText();
 }
 
 public void imprt() {
	 if(modules.contains($moduleId.path)) { return retval; }
	 parseFile($moduleId.path);
 }
}
