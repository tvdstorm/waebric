module test

def main
  home("nickolas");
end

def home(input)
  let
    tds(img) = img(src=img);
    tds(img) = td tds(img);
    tds(img) = tr tds(img);
    tds(img) = table tds(img);
  in
    tds('../image01.jpg);
  end
end

def tds(img)
	h1(value=img);
end