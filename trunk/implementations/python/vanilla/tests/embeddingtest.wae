module embeddingtest

site
  site/embedtest.html: embedtext()
end

def embedtext()
    p@10 " pretext < em "embtext" > posttext ";
    p " pretext < em "embeddingtext" > midtext < em "test"> tailtext";
	p " pre string over
                       multi lines
		<em "embedding txt over multi lines
						" > posttext
	post text over multi lines!";

  p " pretext
    < em "embedding
               text" > real
                            midtext < em "test"> tail
            text";
end
