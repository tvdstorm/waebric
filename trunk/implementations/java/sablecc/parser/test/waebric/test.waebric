module home

import utils

site
  [site/index.html]: home()
end

def menu(menu)
 echo menu;
 ul each (kid: kids)
     menu-item(kid);
end


def menu-item(item)
 if (kids)
   li menu(item);
/* else
   li a(href=link) title;*/
end